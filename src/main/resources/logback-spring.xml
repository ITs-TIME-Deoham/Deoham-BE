<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <property name="APP_NAME" value="deoham"/>
  <property name="LOG_DIR" value="/app/logs"/>

  <!-- Console Appender -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
      <charset>UTF-8</charset>
    </encoder>
  </appender>

  <!-- File Appender (Docker 볼륨 권한 문제로 비활성화) -->
  <!-- Docker 환경에서는 stdout/stderr를 사용하고, Loki/Promtail이 수집 -->
  <!--
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_DIR}/application.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_DIR}/application.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      <maxFileSize>10MB</maxFileSize>
      <maxHistory>10</maxHistory>
      <totalSizeCap>1GB</totalSizeCap>
    </rollingPolicy>
    <encoder>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
      <charset>UTF-8</charset>
    </encoder>
  </appender>
  -->

  <!-- Loki Appender (optional, best-effort) -->
  <appender name="LOKI" class="ch.qos.logback.core.UnsupportedAppenderBase">
    <name>LOKI</name>
  </appender>

  <!-- Spring Boot Default Loggers -->
  <logger name="org.springframework" level="INFO"/>
  <logger name="org.springframework.web" level="INFO"/>
  <logger name="org.hibernate" level="INFO"/>

  <!-- Application Loggers (Docker: console only) -->
  <logger name="com.deoham" additivity="false" level="DEBUG">
    <appender-ref ref="CONSOLE"/>
  </logger>

  <!-- Default Root Logger (Docker: console only) -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
  </root>
</configuration>
