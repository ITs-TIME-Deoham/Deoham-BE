global:
  resolve_timeout: 5m

route:
  receiver: 'null'
  group_by: ['alertname', 'service']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  routes:
    - matchers:
        - severity="critical"
      receiver: 'null'
      repeat_interval: 1h

receivers:
  - name: 'null'
